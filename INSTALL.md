# 仓库维护指南（INSTALL.md）

> 这份文件是给**仓库维护者**（你自己）看的，不是给老师读者看的教程。
> 老师们只需要在 GitHub 上直接阅读 `docs/` 下的章节即可，无需关心这里的内容。

本指南内容：**如何导出 PDF / 网页版**、**如何在线发布与浏览**、**如何添加图片**，以及**文档写作约定**。

---

## 一、导出 PDF 或单文件网页版

项目自带 `export.sh` 脚本，**零依赖**：只需要系统里有 Python 3 和任意一个浏览器（Chrome / Edge 均可），无需安装任何库。

```bash
./export.sh                # 导出完整 PDF 到 dist/
./export.sh --html         # 导出单文件 HTML（适合发布到网页）
./export.sh --file 路径.md  # 只导出某一个章节
./export.sh --out 自定义名   # 自定义输出文件名
```

导出结果：

- **PDF**：`dist/面向教师的AI编码指南.pdf`，含封面、全部分章、分页，适合打印和离线分发。
- **单文件 HTML**：`dist/面向教师的AI编码指南.html`，图片以内联方式嵌入，一个文件即可打开，也可用于发布到网页。
- `dist/` 是构建产物，已加入 `.gitignore`，**不要提交到仓库**；内容改了随时重新导出即可。

> ⚠️ 若某张图片找不到，导出时会打印 `⚠️ 找不到图片` 警告，但不会中断导出——检查一下路径即可。

## 二、在线发布与浏览方式

发布不是这份文档的重点，但作为维护者，有几种方式可以把文档放到网上给老师们看，按「省事程度」从高到低：

### 方式一：直接推 GitHub 在线看（最省事，零配置，推荐）

GitHub 会自动渲染 `.md` 文件——把项目推上去，所有章节在网页上就能直接阅读，**章节之间的链接自动可点**（GitHub 原生支持 `./`、`../` 相对链接），中文文件名也没问题。每篇文档末尾都有「下一章」链接，老师可以顺着读。

改完文件提交推送，刷新页面即生效。

### 方式二：GitHub Pages（免费，样式更好看，可选）

想要正式网址、样式更好看时用。做法：

1. 运行 `./export.sh --html`，得到 `dist/面向教师的AI编码指南.html`（单文件网页版）。
2. 把该 HTML 上传到仓库（或单独建 `gh-pages` 分支）。
3. 在仓库设置（Settings → Pages）里，把发布来源（Source）选成分支（Branch），保存。
4. 等一两分钟，访问 `https://你的用户名.github.io/ai-coding-for-teachers/` 即可。

### 方式三：Cloudflare Pages（可选，国内访问通常更快）

1. 把项目推到 GitHub。
2. 在 [dash.cloudflare.com](https://dash.cloudflare.com) 注册账号（免费）。
3. 进入 **Workers & Pages → Create → Pages → Connect to Git**，选你的仓库。
4. 构建命令（Build command）填 `./export.sh --html`，输出目录（Output directory）填 `dist`。
5. 保存后自动构建，得到一个 `https://xxx.pages.dev` 的网址。之后每次推代码自动更新。

> 💡 三种方式可以同时用，互不冲突。文档本身是 Markdown，随时可以重新导出、重新发布。

## 三、如何添加图片（如软件截图）

1. **图片统一放在根目录 `assets/` 文件夹**（没有就新建一个）。
2. 在章节里用相对路径引用，`alt` 写一句说明（屏幕阅读器也能读）：

   ```markdown
   ![Reasonix 主界面截图](../assets/reasonix-主界面.png)
   ```

   > ⚠️ 路径里的 `../` 是根据「md 文件所在的文件夹层级」来写的：`docs/01-ai-coding-basics/` 下的文件引用 `assets/` 要用 `../../assets/`；`docs/02-tool-guide/` 下用 `../assets/`。
3. 正常导出即可，**无需任何额外操作**：

   - **PDF / 单文件 HTML**：`export.sh` 会自动把图片以 base64 内联进文件，导出后图片照常显示，不需要带着图片文件夹一起拷贝。
   - **GitHub 直接查看**：相对路径的图片在 GitHub 上也能正常显示。

## 四、文档写作约定

写新章节时遵守这些约定，保持全篇一致：

- 文中的 `Reasonix` 指本工具（桌面 GUI 版本）。
- 界面描述以 Reasonix 桌面版 v1.19.x 为基准，不同小版本可能略有差异。
- 需要读者在电脑上操作的地方，都用「👉 动手做」标记。
- 提醒读者注意的地方，都用「⚠️ 注意」标记。
- 每篇文档末尾加「📖 下一章」链接（相对路径），最后一篇加「🏠 返回目录」，让 GitHub 上可以顺着读。
- 读者以中小学老师为主，Windows 系统为主：**不要引入 macOS / Linux 专有概念**（如 `~` 目录、终端命令差异），尽量屏蔽操作系统相关表述。
- 面向教师，类比要贴近课堂（如「校规 / 班规」「大脑 / 工具」）。

## 五、相关文件

| 文件 / 目录 | 作用 |
| --- | --- |
| `docs/` | 教程正文（五部分 + 附录） |
| `templates/` | 可复制的 AGENTS.md 模板文件 |
| `examples/竖式乘法游戏/` | 示例游戏成品（双击 index.html 即玩） |
| `tools/md2html.py` | 导出脚本依赖的 Markdown → HTML 转换器（纯 Python 标准库） |
| `export.sh` | 导出脚本（见本文件第一节） |
| `assets/` | 图片目录（如软件截图），按第三节约放置 |
